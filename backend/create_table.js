const sql = require('mssql'); require('dotenv').config(); sql.connect({user: process.env.DB_USER, password: process.env.DB_PASSWORD, server: process.env.DB_SERVER, database: process.env.DB_NAME, options: {encrypt: false, trustServerCertificate: true}}).then(p => p.query("CREATE TABLE LIC_ROLLBACK_LOG (Id INT IDENTITY(1,1) PRIMARY KEY, IdBitacora INT, Accion NVARCHAR(10), NumeroLicencia NVARCHAR(50), ValorAnterior_Hasta DATE, ValorAnterior_NumDias INT)")).then(() => console.log('Table created')).catch(console.error).finally(()=>process.exit());
